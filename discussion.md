# LunaCalendar 議論メモ

## 目的

LunaCalendar は、カレンダーアプリおよび年間行事予定表で使う日付データを、正確な暦データから生成するためのプロジェクト。

AIだけで暦を作成すると、旧暦・24節気・祝日・行事日の誤りが起きやすい。
そのため、日付の確定情報は信頼できるデータ元から取得し、AIは整形・確認補助として使う方針にする。

## 確定した方針

- 祝祭日は、日本の国民の祝日・休日を対象にする。
- 祝日データは、内閣府が公開する「国民の祝日」CSVを基準にする。
- 24節気、春分、秋分、朔弦望は、国立天文台 暦計算室などの信頼できる暦データで確認する。
- 旧暦日付は、AJNET（日本カレンダー事務局）などの旧暦対応カレンダーで照合する。
- 焼香礼拝日は、毎月の新月・満月にあたる日として、旧暦1日・15日に入れる。
- 仁照大帝殯天日は、旧暦4月14日。焼香礼拝日とは別日扱いにする。

## 作成するテキストファイル

毎月5種類のテキストファイルを作成する。

1. `weekday.txt`
   - 日付・曜日・24節気

2. `youbi.txt`
   - 曜日・日本の国民の祝日・24節気・雑節
   - 日付数字は入れない

3. `kyureki_cal.txt`
   - 旧暦日付
   - 旧暦行事
   - 焼香礼拝日
   - 十干十二支

4. `kyureki.txt`
   - 新暦
   - 旧暦
   - 仏さまの聖誕日や殯天日などの年間行事

5. `cell.txt`
   - 年間行事予定表PDFの1日1セル表示用
   - 上記3ファイルの内容を日付ごとに統合する

## PDFから確認したセル構造

添付された過去の年間行事予定表PDFを確認した結果、1つの日付セルは以下の構造だった。

```text
日にち　祝祭日　24節気　礼拝日　聖誕日など
曜日　　　　　　　　　　　　　　　旧暦
```

確認できた特徴:

- 左上に新暦の日にちが入る。
- 上段には祝日、24節気、雑節、焼香礼拝日、聖誕日、殯天日、大典日などが入る。
- 下段左側に曜日が入る。
- 下段右端に旧暦が入る。
- 行事がない日は、上段は日にちだけになり、中央部分は空白になる。
- 旧暦は毎日表示される。

この構造に合わせて、4つ目の `cell.txt` を追加する。

## cell.txt の基本形式

1日分を2行で表す。

```text
1	[元日]	【小寒】	焼香礼拝日	仁照大帝聖誕日
水							旧12月初2日
2
木							旧12月初3日
3	[彼岸入り]
金							旧12月初4日
```

上段の並び:

```text
日にち	祝日・休日	24節気	雑節	焼香礼拝日	聖誕日・殯天日・大典日など
```

下段の並び:

```text
曜日							旧暦
```

## 今後作るとよいもの

- 暦データを読み込んで5種類のテキストを生成するスクリプト。
- 内閣府の祝日CSVを取り込む処理。
- 24節気・春分・秋分・朔弦望データを取り込む処理。
- 旧暦データをCSVまたはJSONで管理する仕組み。
- 生成後に、`weekday.txt`、`youbi.txt`、`kyureki_cal.txt`、`kyureki.txt`、`cell.txt` の整合性をチェックする検証処理。

## 注意点

- AIに日付を推測させない。
- 旧暦の月初、閏月、旧暦12月最終日は特に確認する。
- 彼岸入り・彼岸明けは春分・秋分を基準に算出する。
- `cell.txt` の曜日と旧暦の間のタブ数は、PDFや表計算ソフトに流し込む時点で調整してよい。

## 追加議論: アプリ化の方向性

最初はスマホで見られるカレンダーや、iPhone/Googleカレンダー連携も検討した。
ただし現段階では、紙で配布している年間行事予定表を作ることが優先。

そのため、第一段階の目的は以下に絞る。

```text
Illustratorに流し込むための 1〜4 のテキストデータを、流れ作業で作れる制作支援ツールにする。
```

現在の位置づけ:

- 一般公開用のカレンダーWebアプリではない。
- まずは紙資料作成用のテキスト生成ツール。
- 将来的に、同じデータをWeb表示、スマホ表示、`.ics` カレンダー配信へ展開できるようにする。

## Illustratorとの関係

現状の紙資料はIllustratorで作成している。
色分けや紙面デザインの品質を考えると、当面はIllustratorを使い続ける。

役割分担:

```text
weekday.txt  -> Illustratorの日付・曜日・24節気レイヤー用
youbi.txt    -> Illustratorの祝日・節気・雑節レイヤー用
kyureki_cal.txt -> Illustratorの旧暦・年間行事レイヤー用
kyureki.txt     -> 新暦・旧暦・年間行事の一覧用
cell.txt        -> 1日1セル表示の確認用・次段階展開用
```

`cell.txt` はIllustratorへ直接流し込む主データではなく、1〜3の内容を日付ごとにまとめて確認するための統合データとして重要。

## 実装済み: LunaCalendar テキスト生成ツール

静的Webアプリとして、以下のファイルを追加済み。

- `index.html`
- `styles.css`
- `app.js`

できること:

- 対象年と対象月を選ぶ。
- 祝日CSV、24節気CSV、旧暦CSV、年間行事CSVを貼り付けまたは読み込む。
- 1か月分または12か月分を生成する。
- `weekday.txt`、`youbi.txt`、`kyureki_cal.txt`、`kyureki.txt`、`cell.txt` を画面表示する。
- 各出力をコピーまたは保存する。

入力CSVの基本形式:

```csv
date,name
2027-01-01,元日
```

```csv
date,lunar_month,lunar_day,leap
2027-01-01,12,24,false
```

```csv
type,month,day,name
lunar,1,1,旧正月節・弥勒祖師聖誕日
lunar-last,12,,旧大晦日
gregorian,1,1,［丁未年］
```

旧暦表記は `rules.md` のルールに沿って整形する。
ただし、旧暦の日付そのもの、24節気の日付、祝日はアプリが推測しない。
確認済みCSVを入力し、それを整形・統合する方針。

## GitHub

リポジトリ:

```text
https://github.com/mahorobach/luna-calendar.git
```

初回コミット:

```text
0ca7a89 Initial LunaCalendar text generator
```

ブランチ:

```text
main
```

## 次にやること

優先順位:

1. 2027年など対象年を決める。
2. 祝日、24節気、旧暦の確認済みCSVを用意する。
3. 1か月分で生成結果とIllustratorへの流し込みを確認する。
4. `weekday.txt`、`youbi.txt`、`kyureki_cal.txt` の形式が既存Illustratorデータと完全に合うか調整する。
5. `cell.txt` で行事・旧暦・焼香礼拝日・彼岸の整合性を確認する。
6. 問題なければ12か月分に広げる。

今後の改善候補:

- CSVのテンプレートファイルを `data/templates/` などに追加する。
- サンプルデータを本番用データと分けて管理する。
- 生成した12か月分をまとめてZIP保存できるようにする。
- 入力不足や旧暦データの欠落をより詳しく警告する。
- Illustrator貼り込み用に、必要なら出力形式をさらに調整する。

## 追加実装: 公式データ取得

祝日と24節気について、公式データ取得機能を追加した。

画面:

- 「公式データ取得」欄を追加。
- 「祝日を取得」ボタンで内閣府の祝日CSVを取得し、`date,name` 形式へ変換する。
- 「24節気を取得」ボタンで国立天文台の暦要項HTMLを取得し、24節気を `date,name` 形式へ変換する。
- 「旧暦を取得」ボタンでJapanese Calendar APIの旧暦データを取得し、`date,lunar_month,lunar_day,leap` 形式へ変換する。
- Cloudflare Pages Functionsの `/api/holidays`、`/api/solar-terms`、`/api/lunar` を使う。

補助スクリプト:

```bash
node tools/fetch-official-data.mjs 2027
```

Cloudflare Pagesで公開後、以下の形式のURLを開く。

```text
https://<project-name>.pages.dev/
```

生成ファイル:

```text
data/generated/2027/holidays.csv
data/generated/2027/solar_terms.csv
```

`file://` で開いたブラウザではCORSなどの制限で公式サイトへ直接アクセスできない場合がある。
画面の公式データ取得ボタンを使う場合は、Cloudflare Pages Functions経由で取得する。
CSVファイルだけを生成する場合はNodeスクリプトを使う。

旧暦については、Japanese Calendar APIから取得する。
ただし旧暦1日・15日、旧正月、旧大晦日、閏月の扱いは、AJNETなどの旧暦対応カレンダーで照合する。

Gemini 3.1 Flash-LiteなどのAIは、日付確定には使わない。
使う場合は、PDFやHTMLからCSV形式へ整える補助、表記ゆれの整理、欠落チェックなどに限定する。
